<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cover Letter - {{companyName}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Times New Roman', serif;
            line-height: 1.6;
            color: #333;
            background: white;
            font-size: 12pt;
        }
        
        .cover-letter {
            max-width: 8.5in;
            margin: 0 auto;
            padding: 1in;
            background: white;
        }
        
        .header {
            margin-bottom: 2rem;
        }
        
        .date {
            text-align: right;
            margin-bottom: 2rem;
            color: #666;
        }
        
        .recipient {
            margin-bottom: 2rem;
        }
        
        .recipient-name {
            font-weight: bold;
            margin-bottom: 0.25rem;
        }
        
        .recipient-title {
            margin-bottom: 0.25rem;
        }
        
        .company-name {
            font-weight: 500;
            margin-bottom: 0.25rem;
        }
        
        .subject {
            margin-bottom: 2rem;
            font-weight: bold;
        }
        
        .content {
            line-height: 1.8;
        }
        
        .paragraph {
            margin-bottom: 1.5rem;
            text-align: justify;
        }
        
        .paragraph:last-child {
            margin-bottom: 0;
        }
        
        .signature-space {
            margin-top: 3rem;
        }
        
        @media print {
            body {
                -webkit-print-color-adjust: exact;
                color-adjust: exact;
            }
            
            .cover-letter {
                margin: 0;
                padding: 0.75in;
            }
        }
    </style>
</head>
<body>
    <div class="cover-letter">
        <!-- Date -->
        <div class="date">
            {{formatDate currentDate}}
        </div>
        
        <!-- Recipient Information -->
        <div class="recipient">
            {{#if hiringManagerName}}
            <div class="recipient-name">{{hiringManagerName}}</div>
            {{/if}}
            <div class="recipient-title">Hiring Manager</div>
            {{#if companyName}}
            <div class="company-name">{{companyName}}</div>
            {{/if}}
        </div>

        <!-- Subject Line -->
        {{#if jobTitle}}
        <div class="subject">
            Re: Application for {{jobTitle}} Position
        </div>
        {{/if}}

        <!-- Letter Content -->
        <div class="content">
            <!-- Opening -->
            {{#if content.opening}}
            <div class="paragraph">
                {{{formatParagraph content.opening}}}
            </div>
            {{/if}}

            <!-- Body Paragraphs -->
            {{#each content.body}}
            {{#if this}}
            <div class="paragraph">
                {{{formatParagraph this}}}
            </div>
            {{/if}}
            {{/each}}

            <!-- Closing -->
            {{#if content.closing}}
            <div class="paragraph">
                {{{formatParagraph content.closing}}}
            </div>
            {{/if}}
        </div>
    </div>
</body>
</html>